---
kind: pipeline
name: ci-tools

steps:
- name: build  
  image: plugins/docker
  settings:
    username: mosstech
    password:
      from_secret: DOCKER_REGISTRY_PASS
    repo: mosstech/ci-tools
    auto_tag: true
  when:
    event: push
    branch: master
